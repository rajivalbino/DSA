#include "gtest/gtest.h"
#include "linked-list.h"

/* Singly Linked List
 * 
 * Attributes:
 * - int size
 * - Node* head
 * - Node* tail
 *
 * Methods:
 * addHead(), addTail(), addAt()
 * remove(data), removeHead(), removeTail(), removeAt(), clear()
 * head(), tail()
 * size(), isEmpty(), 
 */


TEST(LinkedList, addHead) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}